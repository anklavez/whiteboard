version: '2'
services:
    whiteboard-mongodb:
        image: mongo:3.6.3
        ports:
            - "27017:27017"
        command: mongos --configdb whiteboard-mongodb-config
    whiteboard-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    whiteboard-mongodb-config:
        image: mongo:3.6.3
        container_name: whiteboard-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
